# CI is temporarily disabled while the project stabilizes.
# To re-enable, change the branches below back to [main] and uncomment the jobs.
name: ETIP CI

on:
  push:
    branches: [never-run-automatically]

jobs:
  placeholder:
    name: CI Disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI is temporarily disabled. Re-enable in ci.yml when ready."
